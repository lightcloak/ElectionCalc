<UserControl x:Class="ElectionCalc.Views.ControlView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:ElectionCalc.Views"
             mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="300">
    <Grid Background="Blue">
        <StackPanel Margin="10">
            <TextBlock x:Name="txtBlk" TextWrapping="Wrap" Text="Please Wait..." />
            <Button x:Name="btnCast" Content="Cast Your vote" Margin="0,10,0,0" />
            <Button x:Name="btnSummary" Content="Votes Summary" Margin="0,10,0,0" />
            <Button x:Name="btnLogOut" Content="Log Out" Margin="0,10,0,0" />
        </StackPanel>
    </Grid>
</UserControl>
